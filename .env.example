# AWS Bedrock Configuration (Required for LLM features)
# ======================================================
# AWS credentials are required for Bedrock LLM access
# Set these via environment variables or AWS credentials file (~/.aws/credentials)
# 
# Option 1: Environment variables (recommended for production)
AWS_ACCESS_KEY_ID=your_aws_access_key_id
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key
AWS_SESSION_TOKEN=your_session_token  # Optional, only if using temporary credentials
AWS_REGION=us-east-1  # AWS region where Bedrock is available (default: us-east-1)
BEDROCK_MODEL_ID=us.anthropic.claude-3-7-sonnet-20250219-v1:0  # Optional, has default

# Option 2: AWS credentials file
# Create ~/.aws/credentials with:
# [default]
# aws_access_key_id = your_access_key
# aws_secret_access_key = your_secret_key
# 
# And ~/.aws/config with:
# [default]
# region = us-east-1

# External API Keys (Optional but recommended)
# ============================================
# These APIs are used for weather, news, and distance data
# If not set, the system will use fallback methods
OPENWEATHERMAP_API_KEY=your_openweathermap_api_key
NEWSAPI_API_KEY=your_newsapi_api_key
OPENROUTESERVICE_API_KEY=your_openrouteservice_api_key
TAVILY_API_KEY=your_tavily_api_key

# Feature Flags
# =============
FALLBACK_ENABLED=true  # Enable fallback methods when APIs fail
WEB_SEARCH_ENABLED=true  # Enable web search for risk/sourcing agents
